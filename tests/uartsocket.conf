#This configuration default using by client.

#Transmit Protocol Support
comm_protocol = udp,tcp

#Comm Port Configuration
serial_dev = /dev/ttyS1
main_ip = {$router_ip}
udp_port = {$default_udp_port}
tcp_port = {$default_tcp_port}
